# Web Workspace

The frontend is intentionally scaffolded but not yet installed.
It will host the review UI that shows:

- class-level graph by default
- method expansion for a selected type
- change and impact badges
- right-side inspection panel for symbol details and diff summaries

Use this workspace to build the React application once the first graph APIs are ready.
